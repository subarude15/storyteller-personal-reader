name: Silveran
options:
  minimumXcodeGenVersion: 2.38.0
  deploymentTarget:
    iOS: "18.0"
    macOS: "15.0"
    watchOS: "10.0"
    tvOS: "17.0"
configs:
  Debug: debug
  Release: release
packages:
  Silveran:
    path: .
targetTemplates:
  appBase:
    type: application
    productName: "Silveran Reader"
    configFiles:
      Debug: XCodeApps/Configs/Debug.xcconfig
      Release: XCodeApps/Configs/Release.xcconfig
    settings:
      base:
        CURRENT_PROJECT_VERSION: 1
        DEVELOPMENT_TEAM: "$(DEVELOPMENT_TEAM)"
        GENERATE_INFOPLIST_FILE: NO
        MARKETING_VERSION: 0.1
        PRODUCT_NAME: Silveran Reader
        SWIFT_VERSION: 5.10

targets:
  Silveran Reader (macOS):
    templates:
      - appBase
    platform: macOS
    supportedDestinations: [macOS]
    settings:
      base:
        MACOSX_DEPLOYMENT_TARGET: 15.0
        ENABLE_HARDENED_RUNTIME: YES
        CODE_SIGN_ENTITLEMENTS: XCodeApps/SilveranReaderMac.entitlements
        INFOPLIST_FILE: XCodeApps/Info-macOS.plist
        PRODUCT_BUNDLE_IDENTIFIER: "$(APP_BUNDLE_ID)"
        #INFOPLIST_KEY_CFBundleDisplayName: Silveran Reader
        #INFOPLIST_KEY_CFBundleName: Silveran Reader
        # Unknown why this redundant definition is needed for XCode to honor the app name
        #SWIFT_UPCOMING_FEATURE_NONSENDING_NONISOLATING_BY_DEFAULT: YES
    info:
      path: XCodeApps/Info-macOS.plist
      properties:
        CFBundleName: "ink+amp"
        CFBundleIdentifier: "$(PRODUCT_BUNDLE_IDENTIFIER)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundlePackageType: "$(PRODUCT_BUNDLE_PACKAGE_TYPE)"
        CFBundleExecutable: "$(EXECUTABLE_NAME)"
        KEYCHAIN_ACCESS_GROUP: "$(KEYCHAIN_ACCESS_GROUP)"
        KEYCHAIN_SERVICE: "$(KEYCHAIN_SERVICE)"
        SILVERAN_WIDGET_APP_GROUP: "$(MAC_APP_GROUP_ID)"
        LSApplicationCategoryType: "public.app-category.books"
        NSLocalNetworkUsageDescription: "ink+amp needs local network access to connect to your Storyteller server."
        ITSAppUsesNonExemptEncryption: false
        ATSApplicationFontsPath: "fonts"
        CFBundleURLTypes:
          - CFBundleURLName: com.punkrally.reader
            CFBundleURLSchemes:
              - punkrally
    sources:
      - path: XCodeApps
        includes:
          - "**/*.swift"
      - path: XCodeApps/Assets.xcassets
        type: assetcatalog
      - path: XCodeApps/PrivacyInfo.xcprivacy
        buildPhase: resources
    dependencies:
      - package: Silveran
        product: SilveranAppleKit
      - package: Silveran
        product: SilveranContentServer
      - package: Silveran
        product: SilveranReadaloud
      - target: "Silveran Reader Widgets (macOS)"
        embed: true

  Silveran Reader (iOS):
    templates:
      - appBase
    platform: iOS
    settings:
      base:
        IPHONEOS_DEPLOYMENT_TARGET: 18.0
        CODE_SIGN_ENTITLEMENTS: XCodeApps/SilveranReaderIos.entitlements
        CODE_SIGN_STYLE: Automatic
        INFOPLIST_FILE: XCodeApps/Info-iOS.plist
        PRODUCT_BUNDLE_IDENTIFIER: "$(APP_BUNDLE_ID)"
        #INFOPLIST_KEY_CFBundleDisplayName: Silveran Reader
        INFOPLIST_KEY_CFBundleDisplayName: "ink+amp"
        SUPPORTS_MAC_DESIGNED_FOR_IPHONE_IPAD: NO
        #SWIFT_UPCOMING_FEATURE_NONSENDING_NONISOLATING_BY_DEFAULT: YES
    info:
      path: XCodeApps/Info-iOS.plist
      properties:
        CFBundleName: "ink+amp"
        CFBundleDisplayName: "ink+amp"
        CFBundleIdentifier: "$(PRODUCT_BUNDLE_IDENTIFIER)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundlePackageType: "$(PRODUCT_BUNDLE_PACKAGE_TYPE)"
        CFBundleExecutable: "$(EXECUTABLE_NAME)"
        KEYCHAIN_ACCESS_GROUP: "$(KEYCHAIN_ACCESS_GROUP)"
        KEYCHAIN_SERVICE: "$(KEYCHAIN_SERVICE)"
        SILVERAN_WIDGET_APP_GROUP: "$(APP_GROUP_ID)"
        LSApplicationCategoryType: "public.app-category.books"
        NSLocalNetworkUsageDescription: "ink+amp needs local network access to connect to your Storyteller server and ad-strip worker."
        NSAppTransportSecurity:
          NSAllowsLocalNetworking: true
        CFBundleURLTypes:
          - CFBundleURLName: com.punkrally.reader
            CFBundleURLSchemes:
              - punkrally
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UIBackgroundModes:
          - audio
          - fetch
        BGTaskSchedulerPermittedIdentifiers:
          - com.kyonifer.silveran.progresssync.refresh
        ITSAppUsesNonExemptEncryption: false
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: true
          UISceneConfigurations:
            CPTemplateApplicationSceneSessionRoleApplication:
              - UISceneConfigurationName: CarPlay
                UISceneDelegateClassName: SilveranAppleKit.CarPlaySceneDelegate
    sources:
      - path: XCodeApps
        includes:
          - "**/*.swift"
      - path: XCodeApps/Assets.xcassets
        type: assetcatalog
      - path: XCodeApps/PrivacyInfo.xcprivacy
        buildPhase: resources
    dependencies:
      - package: Silveran
        product: SilveranAppleKit
      - package: Silveran
        product: SilveranReadaloud
      - target: "Silveran Reader Widgets (iOS)"
        embed: true

  Silveran Reader Sideload (iOS):
    templates:
      - appBase
    platform: iOS
    settings:
      base:
        IPHONEOS_DEPLOYMENT_TARGET: 18.0
        CODE_SIGN_ENTITLEMENTS: XCodeApps/SilveranReaderSideload.entitlements
        CODE_SIGN_STYLE: Automatic
        INFOPLIST_FILE: XCodeApps/Info-iOS-Sideload.plist
        PRODUCT_BUNDLE_IDENTIFIER: "$(APP_BUNDLE_ID)"
        INFOPLIST_KEY_CFBundleDisplayName: "ink+amp"
        SUPPORTS_MAC_DESIGNED_FOR_IPHONE_IPAD: NO
    info:
      path: XCodeApps/Info-iOS-Sideload.plist
      properties:
        CFBundleName: "ink+amp"
        CFBundleDisplayName: "ink+amp"
        CFBundleIdentifier: "$(PRODUCT_BUNDLE_IDENTIFIER)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundlePackageType: "$(PRODUCT_BUNDLE_PACKAGE_TYPE)"
        CFBundleExecutable: "$(EXECUTABLE_NAME)"
        # KEYCHAIN_ACCESS_GROUP omitted: free AltStore signing can't use the
        # shared $(AppIdentifierPrefix) group; SecurityKeychainStore falls back
        # to the default (app-keyed) access group when this Info key is absent.
        KEYCHAIN_SERVICE: "$(KEYCHAIN_SERVICE)"
        # Literal App Group (not $(APP_GROUP_ID)): unsigned IPA must not ship
        # unexpanded build-setting placeholders into Info / AltStore resign.
        SILVERAN_WIDGET_APP_GROUP: group.com.punkrally.reader
        LSApplicationCategoryType: "public.app-category.books"
        NSLocalNetworkUsageDescription: "ink+amp needs local network access to connect to your Storyteller server and ad-strip worker."
        NSAppTransportSecurity:
          NSAllowsLocalNetworking: true
        CFBundleURLTypes:
          - CFBundleURLName: com.punkrally.reader
            CFBundleURLSchemes:
              - punkrally
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UIBackgroundModes:
          - audio
          - fetch
        BGTaskSchedulerPermittedIdentifiers:
          - com.kyonifer.silveran.progresssync.refresh
        ITSAppUsesNonExemptEncryption: false
    sources:
      - path: XCodeApps
        includes:
          - "**/*.swift"
      - path: XCodeApps/Assets.xcassets
        type: assetcatalog
      - path: XCodeApps/PrivacyInfo.xcprivacy
        buildPhase: resources
    dependencies:
      - package: Silveran
        product: SilveranAppleKit
      - package: Silveran
        product: SilveranReadaloud
      - target: "Silveran Reader Widgets (iOS)"
        embed: true
  Silveran Reader Widgets (iOS):
    type: app-extension
    platform: iOS
    configFiles:
      Debug: XCodeApps/Configs/Debug.xcconfig
      Release: XCodeApps/Configs/Release.xcconfig
    settings:
      base:
        IPHONEOS_DEPLOYMENT_TARGET: 18.0
        APPLICATION_EXTENSION_API_ONLY: YES
        CODE_SIGN_ENTITLEMENTS: XCodeApps/SilveranReaderWidgets.entitlements
        CODE_SIGN_STYLE: Automatic
        CURRENT_PROJECT_VERSION: 1
        DEVELOPMENT_TEAM: "$(DEVELOPMENT_TEAM)"
        INFOPLIST_FILE: XCodeApps/Info-Widgets-iOS.plist
        MARKETING_VERSION: 0.1
        PRODUCT_BUNDLE_IDENTIFIER: "$(APP_BUNDLE_ID).widgets"
        PRODUCT_NAME: Silveran Reader Widgets
        SWIFT_VERSION: 5.10
    info:
      path: XCodeApps/Info-Widgets-iOS.plist
      properties:
        CFBundleName: "Silveran Reader Widgets"
        CFBundleDisplayName: "ink+amp"
        CFBundleIdentifier: "$(PRODUCT_BUNDLE_IDENTIFIER)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundlePackageType: "$(PRODUCT_BUNDLE_PACKAGE_TYPE)"
        CFBundleExecutable: "$(EXECUTABLE_NAME)"
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
        # Literal — must match Sideload + widgets entitlements for AltStore.
        SILVERAN_WIDGET_APP_GROUP: group.com.punkrally.reader
    sources:
      # Crash-proof Sideload path: only SideloadContinueWidget.swift (no AppIntents).
      # Old ContinueWidget + Library stay on disk for paid/SideStore; compiling them
      # into this extension pulls AudioPlaybackIntent metadata AltStore can't sign.
      - path: SilveranWidgets/Sources/SideloadContinueWidget.swift
    dependencies:
      - package: Silveran
        product: SilveranAppleWidgets
  Silveran Reader Widgets (macOS):
    type: app-extension
    platform: macOS
    configFiles:
      Debug: XCodeApps/Configs/Debug.xcconfig
      Release: XCodeApps/Configs/Release.xcconfig
    settings:
      base:
        MACOSX_DEPLOYMENT_TARGET: 15.0
        APPLICATION_EXTENSION_API_ONLY: YES
        ENABLE_HARDENED_RUNTIME: YES
        CODE_SIGN_ENTITLEMENTS: XCodeApps/SilveranReaderWidgetsMac.entitlements
        CODE_SIGN_STYLE: Automatic
        CURRENT_PROJECT_VERSION: 1
        DEVELOPMENT_TEAM: "$(DEVELOPMENT_TEAM)"
        INFOPLIST_FILE: XCodeApps/Info-Widgets-macOS.plist
        MARKETING_VERSION: 0.1
        PRODUCT_BUNDLE_IDENTIFIER: "$(APP_BUNDLE_ID).widgets"
        PRODUCT_NAME: Silveran Reader Widgets
        SWIFT_VERSION: 5.10
    info:
      path: XCodeApps/Info-Widgets-macOS.plist
      properties:
        CFBundleName: "Silveran Reader Widgets"
        CFBundleDisplayName: "ink+amp"
        CFBundleIdentifier: "$(PRODUCT_BUNDLE_IDENTIFIER)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundlePackageType: "$(PRODUCT_BUNDLE_PACKAGE_TYPE)"
        CFBundleExecutable: "$(EXECUTABLE_NAME)"
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
        SILVERAN_WIDGET_APP_GROUP: "$(MAC_APP_GROUP_ID)"
    sources:
      - path: SilveranWidgets/Sources
    dependencies:
      - package: Silveran
        product: SilveranAppleWidgets
  Silveran Reader (watchOS):
    templates:
      - appBase
    platform: watchOS
    settings:
      base:
        WATCHOS_DEPLOYMENT_TARGET: 10.0
        CODE_SIGN_ENTITLEMENTS: XCodeApps/SilveranReaderWatch.entitlements
        CODE_SIGN_STYLE: Automatic
        INFOPLIST_FILE: XCodeApps/Info-watchOS.plist
        PRODUCT_BUNDLE_IDENTIFIER: "$(WATCH_APP_BUNDLE_ID)"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
    info:
      path: XCodeApps/Info-watchOS.plist
      properties:
        CFBundleName: "ink+amp"
        CFBundleDisplayName: "ink+amp"
        CFBundleIdentifier: "$(PRODUCT_BUNDLE_IDENTIFIER)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundlePackageType: "$(PRODUCT_BUNDLE_PACKAGE_TYPE)"
        CFBundleExecutable: "$(EXECUTABLE_NAME)"
        KEYCHAIN_ACCESS_GROUP: "$(KEYCHAIN_ACCESS_GROUP)"
        KEYCHAIN_SERVICE: "$(KEYCHAIN_SERVICE)"
        WKApplication: true
        UIBackgroundModes:
          - audio
        WKCompanionAppBundleIdentifier: "$(APP_BUNDLE_ID)"
    sources:
      - path: XCodeApps/EntryPointStub.swift
      - path: XCodeApps/WatchAssets.xcassets
        type: assetcatalog
    dependencies:
      - package: Silveran
        product: SilveranAppleKit
  Silveran Reader (tvOS):
    templates:
      - appBase
    platform: tvOS
    settings:
      base:
        TVOS_DEPLOYMENT_TARGET: 17.0
        CODE_SIGN_ENTITLEMENTS: XCodeApps/SilveranReaderTV.entitlements
        CODE_SIGN_STYLE: Automatic
        INFOPLIST_FILE: XCodeApps/Info-tvOS.plist
        PRODUCT_BUNDLE_IDENTIFIER: "$(APP_BUNDLE_ID)"
        ASSETCATALOG_COMPILER_APPICON_NAME: "App Icon"
    info:
      path: XCodeApps/Info-tvOS.plist
      properties:
        CFBundleName: "ink+amp"
        CFBundleDisplayName: "ink+amp"
        CFBundleIdentifier: "$(PRODUCT_BUNDLE_IDENTIFIER)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundlePackageType: "$(PRODUCT_BUNDLE_PACKAGE_TYPE)"
        CFBundleExecutable: "$(EXECUTABLE_NAME)"
        KEYCHAIN_ACCESS_GROUP: "$(KEYCHAIN_ACCESS_GROUP)"
        KEYCHAIN_SERVICE: "$(KEYCHAIN_SERVICE)"
        LSApplicationCategoryType: "public.app-category.books"
        UILaunchScreen: {}
        UIUserInterfaceStyle: Automatic
        UIBackgroundModes:
          - audio
        ITSAppUsesNonExemptEncryption: false
    sources:
      - path: XCodeApps/EntryPointStub.swift
      - path: XCodeApps/TVAssets.xcassets
        type: assetcatalog
    dependencies:
      - package: Silveran
        product: SilveranAppleKit
schemes:
  "Silveran Reader (macOS)":
    shared: true
    build:
      targets:
        "Silveran Reader (macOS)": all
        "Silveran Reader Widgets (macOS)": all
    run:
      config: Debug
      executable: "Silveran Reader (macOS)"
    test:
      config: Debug
      targets: []
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
  "Silveran Reader (iOS)":
    shared: true
    build:
      targets:
        "Silveran Reader (iOS)": all
        "Silveran Reader Widgets (iOS)": all
    run:
      config: Debug
      executable: "Silveran Reader (iOS)"
    test:
      config: Debug
      targets: []
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
  "Silveran Reader Sideload (iOS)":
    shared: true
    build:
      targets:
        "Silveran Reader Sideload (iOS)": all
        "Silveran Reader Widgets (iOS)": all
    run:
      config: Debug
      executable: "Silveran Reader Sideload (iOS)"
    test:
      config: Debug
      targets: []
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
  "Silveran Reader (watchOS)":
    shared: true
    build:
      targets:
        "Silveran Reader (watchOS)": all
    run:
      config: Debug
      executable: "Silveran Reader (watchOS)"
    test:
      config: Debug
      targets: []
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
  "Silveran Reader (tvOS)":
    shared: true
    build:
      targets:
        "Silveran Reader (tvOS)": all
    run:
      config: Debug
      executable: "Silveran Reader (tvOS)"
    test:
      config: Debug
      targets: []
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
