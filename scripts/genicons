#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "$(readlink -f -- "${BASH_SOURCE[0]}")")" && pwd)"
cd "${SCRIPT_DIR}/.."

ASSETS_DIR="XCodeApps/Assets.xcassets/AppIcon.appiconset"
WATCH_ASSETS_DIR="XCodeApps/WatchAssets.xcassets/AppIcon.appiconset"
TV_ASSETS_DIR="XCodeApps/TVAssets.xcassets/App Icon.brandassets"
STORYTELLER_LOGO="XCodeApps/Assets.xcassets/StorytellerLogo.imageset/Storyteller_Logo.png"
MAC_MASK="SilveranKit/Sources/Kit/Resources/assets/icons/macos_icon_mask.png"

if [ ! -f "$STORYTELLER_LOGO" ] && [ ! -f "icons/twin-pages-final.png" ]; then
    echo "Error: Storyteller_Logo.png and icons/twin-pages-final.png both missing from project"
    exit 1
fi

if ! command -v magick &> /dev/null; then
    echo "Error: ImageMagick not found. Install with: brew install imagemagick"
    exit 1
fi

echo "Generating app icons from twin-pages-final.png..."

SRGB_PROFILE="/System/Library/ColorSync/Profiles/sRGB Profile.icc"
ICON_WORK_DIR="$(mktemp -d)"
trap 'rm -rf "$ICON_WORK_DIR"' EXIT
MAIN_ICON="${ICON_WORK_DIR}/twin_pages_app_icon.png"
ANDROID_FOREGROUND="${ASSETS_DIR}/icon_android_foreground.png"

SOURCE_ICON="icons/twin-pages-final.png"
if [ ! -f "$SOURCE_ICON" ]; then
    echo "Note: icons/twin-pages-final.png missing; falling back to Storyteller_Logo.png"
    SOURCE_ICON="$STORYTELLER_LOGO"
fi

if [[ "$SOURCE_ICON" == *"twin-pages"* ]]; then
    # twin-pages-final.png is full 1280x720 art (dark bg + centered book + bottom glow).
    # Scale-to-cover 1024, then center-crop to the iOS square.
    magick "${SOURCE_ICON}" -auto-orient -resize '1024x1024^' -gravity center -extent 1024x1024 \
        -colorspace sRGB -profile "${SRGB_PROFILE}" "${MAIN_ICON}"
else
    magick -size 1024x1024 gradient:'#fbfffe-#fffbee' \
        \( "${SOURCE_ICON}" -trim +repage -resize 700x700 \) \
        -gravity center -compose over -composite \
        -colorspace sRGB -profile "${SRGB_PROFILE}" "${MAIN_ICON}"
fi

magick "${MAIN_ICON}" "${ASSETS_DIR}/icon_1024x1024.png"
magick "${MAIN_ICON}" "${WATCH_ASSETS_DIR}/icon_1024x1024.png"
magick -size 2048x2048 canvas:none \
    \( "${STORYTELLER_LOGO}" -trim +repage -resize 1024x1024 \) \
    -gravity center -compose over -composite "${ANDROID_FOREGROUND}"

if [ ! -f "${MAC_MASK}" ]; then
    echo "Error: macOS app icon mask not found at ${MAC_MASK}"
    exit 1
fi

generate_mac_icon() {
    local canvas_size=$1
    local output_file=$2
    local icon_size=$(echo "$canvas_size * 824 / 1024" | bc)

    local tmp_resized="${ICON_WORK_DIR}/mac_resized_${canvas_size}.png"
    local tmp_mask="${ICON_WORK_DIR}/mac_mask_${canvas_size}.png"
    local tmp_masked="${ICON_WORK_DIR}/mac_masked_${canvas_size}.png"

    magick "${MAIN_ICON}" -resize ${icon_size}x${icon_size} -colorspace sRGB -profile "${SRGB_PROFILE}" "${tmp_resized}"
    magick "${MAC_MASK}" -resize ${icon_size}x${icon_size} "${tmp_mask}"
    magick "${tmp_resized}" \( "${tmp_mask}" -channel A -separate +channel \) -compose CopyOpacity -composite "${tmp_masked}"
    magick "${tmp_masked}" -gravity center -background none -extent ${canvas_size}x${canvas_size} "${output_file}"

    rm -f "${tmp_resized}" "${tmp_mask}" "${tmp_masked}"
}

generate_mac_icon 16 "${ASSETS_DIR}/icon_mac_16.png"
generate_mac_icon 32 "${ASSETS_DIR}/icon_mac_32.png"
generate_mac_icon 64 "${ASSETS_DIR}/icon_mac_64.png"
generate_mac_icon 128 "${ASSETS_DIR}/icon_mac_128.png"
generate_mac_icon 256 "${ASSETS_DIR}/icon_mac_256.png"
generate_mac_icon 512 "${ASSETS_DIR}/icon_mac_512.png"
generate_mac_icon 1024 "${ASSETS_DIR}/icon_mac_1024.png"

rm -f "${ASSETS_DIR}/icon_1024x1024_mac.png"

generate_tv_icon() {
    local width=$1
    local height=$2
    local output_file=$3
    magick "${MAIN_ICON}" -resize x${height} -gravity center -background 'srgb(40,40,40)' -extent ${width}x${height} -colorspace sRGB -profile "${SRGB_PROFILE}" -alpha off "${output_file}"
}

generate_tv_icon 400 240 "${TV_ASSETS_DIR}/App Icon.imagestack/Back.imagestacklayer/Content.imageset/back.png"
generate_tv_icon 800 480 "${TV_ASSETS_DIR}/App Icon.imagestack/Back.imagestacklayer/Content.imageset/back@2x.png"
generate_tv_icon 400 240 "${TV_ASSETS_DIR}/App Icon.imagestack/Front.imagestacklayer/Content.imageset/front.png"
generate_tv_icon 800 480 "${TV_ASSETS_DIR}/App Icon.imagestack/Front.imagestacklayer/Content.imageset/front@2x.png"
generate_tv_icon 1280 768 "${TV_ASSETS_DIR}/App Icon - App Store.imagestack/Back.imagestacklayer/Content.imageset/back_store.png"
generate_tv_icon 1280 768 "${TV_ASSETS_DIR}/App Icon - App Store.imagestack/Front.imagestacklayer/Content.imageset/front_store.png"

generate_tv_shelf() {
    local width=$1
    local height=$2
    local output_file=$3
    magick "${MAIN_ICON}" -resize x${height} -gravity center -background 'srgb(40,40,40)' -extent ${width}x${height} -colorspace sRGB -profile "${SRGB_PROFILE}" -alpha off "${output_file}"
}

generate_tv_shelf 1920 720 "${TV_ASSETS_DIR}/Top Shelf Image.imageset/shelf.png"
generate_tv_shelf 3840 1440 "${TV_ASSETS_DIR}/Top Shelf Image.imageset/shelf@2x.png"
generate_tv_shelf 2320 720 "${TV_ASSETS_DIR}/Top Shelf Image Wide.imageset/shelf_wide.png"
generate_tv_shelf 4640 1440 "${TV_ASSETS_DIR}/Top Shelf Image Wide.imageset/shelf_wide@2x.png"

echo "Generated all app icon sizes"

# sudo rm -rf /Library/Caches/com.apple.iconservices.store
